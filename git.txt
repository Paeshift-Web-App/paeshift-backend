
git add .
git commit -m "consumer now working"
git push origin backend

git pull origin main
git pull https://github.com/Paeshift-Web-App/paeshift-backend/tree/main

daphne -b 0.0.0.0 -p 8000 payshift.asgi:application --websocket_timeout 60
python -m daphne -b 0.0.0.0 -p 8000 payshift.asgi:application


git push -f origin main
git push origin backend --force
git push origin backend --force-with-lease

git checkout backend  #Switch to the Master Branch

git clone --branch main https://github.com/Paeshift-Web-App/paeshift-backend.git

git init
git add .
git commit -m "Alert adjusted,"
git branch -M main
git remote add origin https://github.com/skillyboy/SanDiegoEcommerce.git
git push -u origin main


git remote add origin https://github.com/Paeshift-Web-App/paeshift-frontend.git

git branch -M main
git push -u origin main